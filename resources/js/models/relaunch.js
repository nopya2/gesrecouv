class Relaunch{
    constructor(id, mode_relaunch = '', comment = '') {
        this.id = id;
        this.mode_relaunch = mode_relaunch;
        this.comment = comment;
     }
}

export default Relaunch
